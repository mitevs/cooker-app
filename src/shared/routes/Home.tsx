import React from 'react';
import DefaultLayout from '@shared/layouts/default/Layout';

const Home = () => (
    <DefaultLayout>
        <h1>Welcome Home!</h1>
    </DefaultLayout>
);

export default Home;
